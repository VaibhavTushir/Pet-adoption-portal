<!DOCTYPE html>
<html>
<head>
  <title>Admin Dashboard | System Logs</title>
  <link rel="icon" type="image/x-icon" href="/images/paw.ico" />
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
  <link rel="preconnect" href="https://fonts.gstatic.com/" crossorigin />
  <link
    rel="stylesheet"
    as="style"
    onload="this.rel='stylesheet'"
    href="https://fonts.googleapis.com/css2?display=swap&family=Noto+Sans:wght@400;500;700;900&family=Plus+Jakarta+Sans:wght@400;500;700;800"
  />
</head>
<body class="bg-white min-h-screen" style='font-family: "Plus Jakarta Sans", "Noto Sans", sans-serif;'>
  <!-- Admin Dashboard Header with Beige Background -->
  <header class="sticky top-0 z-50 flex items-center justify-between border-b border-solid border-b-black px-10 py-3 shadow-lg" style="background-color: #f8f5f0;">
  <div class="flex items-center gap-4 text-[#121617]">
    <img src="/images/logo.png" alt="Pawsitive Match Logo" style="height:40px;" class="inline-block align-middle" />
    <h2 class="text-[#121617] text-lg font-bold leading-tight tracking-[-0.015em]">Pawsitive Match || Admin Dashboard</h2>
  </div>
  <form action="/logout" method="POST">
    <button
      class="flex min-w-[84px] max-w-[480px] cursor-pointer items-center justify-center overflow-hidden rounded-xl h-10 px-4 bg-[#f1f3f4] text-[#121617] text-sm font-bold leading-normal tracking-[0.015em] border border-black"
      type="submit"
    >
      <span class="truncate">Logout</span>
    </button>
  </form>
</header>


  <!-- Main Content -->
  <main class="max-w-6xl mx-auto py-10 px-4">
    <h1 class="text-3xl font-extrabold mb-6 text-[#121617] text-center">System Logs</h1>
    <div class="overflow-x-auto rounded-xl border border-black bg-white shadow">
      <table class="min-w-full divide-y divide-black border border-black">
        <thead style="background-color: #f8f5f0;">
          <tr>
            <th class="px-4 py-3 text-left text-[#121617] text-sm font-semibold border-b border-black">Log ID</th>
            <th class="px-4 py-3 text-left text-[#121617] text-sm font-semibold border-b border-black">Timestamp</th>
            <th class="px-4 py-3 text-left text-[#121617] text-sm font-semibold border-b border-black">Table</th>
            <th class="px-4 py-3 text-left text-[#121617] text-sm font-semibold border-b border-black">Record ID</th>
            <th class="px-4 py-3 text-left text-[#121617] text-sm font-semibold border-b border-black">Action Type</th>
          </tr>
        </thead>
        <tbody>
          <% if (logs && logs.length > 0) { %>
            <% logs.forEach(function(log) { %>
              <tr style="background-color: #fff;">
                <td class="px-4 py-2 text-[#677c83] text-sm border-b border-black"><%= log.log_id %></td>
                <td class="px-4 py-2 text-[#677c83] text-sm border-b border-black"><%= log.timestamp ? new Date(log.timestamp).toLocaleString() : '—' %></td>
                <td class="px-4 py-2 text-[#677c83] text-sm border-b border-black"><%= log.table_name %></td>
                <td class="px-4 py-2 text-[#677c83] text-sm border-b border-black"><%= log.record_id %></td>
                <td class="px-4 py-2 text-[#677c83] text-sm border-b border-black"><%= log.action_type %></td>
              </tr>
            <% }); %>
          <% } else { %>
            <tr style="background-color: #fff;">
              <td colspan="5" class="px-4 py-8 text-center text-[#677c83] text-base border-b border-black">No logs found.</td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </main>
</body>
</html>
